name: build
on: [push, pull_request]
jobs:
  ci:
    name: run
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@master
    - name: build
      run: bash ./bootstrap
    - name: CI
      run: bash ./test
